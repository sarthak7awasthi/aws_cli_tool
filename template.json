{
    "messages": [
        {
            "messageType": "UPLOAD",
            "fileUrl": "{{fileUrl}}",
            "uuid": "{{uuid}}",
            "publisherId": "default_publisher",
            "timestamp": "{{timestamp}}"
        }
    ]
}
